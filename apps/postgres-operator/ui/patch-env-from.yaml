apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-operator-ui
spec:
  template:
    spec:
      containers:
        - name: postgres-operator-ui
          envFrom:
            - configMapRef:
                name: envs