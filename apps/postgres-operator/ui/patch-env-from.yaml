apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: postgres-operator
  labels:
    name: postgres-operator-ui
spec:
  template:
    spec:
      containers:
        - name: "*"
          envFrom:
            - configMapRef:
                name: envs